# {{appName}}

Full-stack: Backend (Express @5000) + Frontend (Next.js + HeroUI @3000)

## Chạy
```bash
cp .env.example .env
cd backend && cp .env.example .env && npm i && cd ..
cd frontend && cp .env.example .env && npm i && cd ..
npm run dev