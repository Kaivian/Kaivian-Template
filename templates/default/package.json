{
  "name": "kaivian_template_app",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "start": "concurrently -n server,client -c blue,green \"npm start -w server\" \"npm start -w client\"",
    "test": "node --test",
    "test:server": "npm run test -w server",
    "test:watch": "node --test --watch"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}
